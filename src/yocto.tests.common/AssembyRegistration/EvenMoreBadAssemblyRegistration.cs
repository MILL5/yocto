using System;

namespace yocto.tests.evenmorebad
{
    public static class AssemblyRegistration
    {
        private static void Initialize(IContainer container)
        {
        }

        public static void Init()
        {
            Initialize(null);
        }
    }
}
